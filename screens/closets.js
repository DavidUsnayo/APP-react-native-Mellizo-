import { StyleSheet, Text, View, Button, TouchableOpacity} from 'react-native';
import { Categoria } from '../componentes/categoria';

export function Closets() {
    return (
        <Categoria valor='closets'/>
    );
}
